// إرسال البريد باستخدام EmailJS
window.emailService = {
  sendNotificationEmail: async function(applicantData, responsible, templateType = 'confirm') {
    // ... يمكنك نقل دالة sendNotificationEmail من script.js هنا ...
    // يجب أن تستخدم window.APP_EMAIL_CONFIG
  }
};
