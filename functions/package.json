{
  "name": "my-first-website-functions",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:sendEmailNotification",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-functions": "^5.1.1",
    "@sendgrid/mail": "^8.1.3"
  }
}
